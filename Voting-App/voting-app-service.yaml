# api version :happy
apiVersion: v1
kind: Service
metadata:
  name: adservice-svc
  labels:
    app: adservice
    env: pro
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005
  selector:
    app: adservice
    env: pro
