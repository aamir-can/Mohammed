- name: "Execute a script on all web server nodes"
  hosts: web_nodes
  tasks:
    - name: "Execute a script on all web server nodes"
      script: /tmp/install_script.sh
    - name: "start httpd services on all web servers"
      service: "name=httpd state=started"
